<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.PresentationMode="ModalAnimated"
             x:Class="Security_Map.Views.Usuarios.CadastroUserView"
             >
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="3" Padding="15">
                <Label Text="Nome de Usuário:" FontSize="Medium" />
                <Entry Text="{Binding Username}" FontSize="Medium" />

                <Label Text="Senha:" FontSize="Medium" />
                <Entry Text="{Binding PasswordString}" FontSize="Medium" />

                <StackLayout Orientation="Horizontal">
                    <Button Text="Salvar" Command="{Binding SalvarCommand}"
                            HorizontalOptions="FillAndExpand"></Button>

                    <Button Text="Cancelar" Command="{Binding CancelarCommand}"
                            HorizontalOptions="FillAndExpand"></Button>


                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>